Flask==2.3.3
google-generativeai==0.1.0
moviepy==1.0.3
transformers==4.33.2
pydub==0.25.1
PyPDF2==3.0.1
SpeechRecognition==3.10.0
torch
torchvision
torchaudio

# Note: ffmpeg is required for moviepy and pydub.
# Install ffmpeg via package manager:
# For Ubuntu: sudo apt-get install ffmpeg
# For macOS (with Homebrew): brew install ffmpeg
# For Windows: Download from https://ffmpeg.org/download.html and add to PATH
